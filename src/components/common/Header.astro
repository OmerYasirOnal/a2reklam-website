---
import { SITE_TITLE } from '../../consts';

interface Props {
  lang?: string;
}

const { lang = 'tr' } = Astro.props;

const navItems = {
  tr: [
    { href: '/', label: 'Anasayfa' },
    { href: '/hakkimizda/', label: 'Hakkımızda' },
    { href: '/hizmetler/', label: 'Hizmetler' },
    { href: '/referanslar/', label: 'Referanslar' },
    { href: '/iletisim/', label: 'İletişim' },
  ],
  en: [
    { href: '/en/', label: 'Home' },
    { href: '/en/about-us/', label: 'About Us' },
    { href: '/en/services/', label: 'Services' },
    { href: '/en/portfolio/', label: 'Portfolio' },
    { href: '/en/contact/', label: 'Contact' },
  ]
};

const items = lang === 'en' ? navItems.en : navItems.tr;
const switchLangLink = lang === 'en' ? '/' : '/en/'; // Naive switcher for now
const switchLangLabel = lang === 'en' ? 'TR' : 'EN';
---

<header class="sticky top-0 z-50 bg-secondary/95 backdrop-blur-sm border-b border-white/10 transition-all duration-300">
  <div class="container mx-auto px-4 h-20 flex items-center justify-between">
    <a href={lang === 'en' ? '/en/' : '/'} class="font-heading font-black text-2xl tracking-tighter text-white flex items-center gap-2">
      <span class="text-primary text-3xl">A2</span> REKLAM
    </a>

    <nav class="hidden md:flex items-center gap-8">
      {items.map(item => (
        <a href={item.href} class="text-sm font-medium text-gray-300 hover:text-primary transition-colors uppercase tracking-wide">
          {item.label}
        </a>
      ))}
    </nav>

    <div class="flex items-center gap-4">
      <a href={switchLangLink} class="text-xs font-bold border border-white/20 px-3 py-1.5 rounded text-gray-300 hover:text-white hover:border-white transition-colors">
        {switchLangLabel}
      </a>
      <a href={lang === 'en' ? '/en/get-quote/' : '/teklif-al/'} class="hidden md:inline-flex bg-primary text-secondary px-6 py-2.5 rounded text-sm font-bold hover:bg-white hover:text-secondary transition-all transform hover:-translate-y-0.5 shadow-lg shadow-primary/20">
        {lang === 'en' ? 'Get Quote' : 'Teklif Al'}
      </a>
    </div>
  </div>
</header>
