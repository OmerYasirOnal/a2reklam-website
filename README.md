# A2 Reklam Astro Rebuild

This is the modern, bilingual (TR/EN) static-first website for a2reklam.com, built with Astro 5.

## 🚀 Quick Start

1. **Install dependencies:**
   ```bash
   npm install
   ```

2. **Run the image pipeline:**
   (Crucial for generating the manifest and optimized WebP assets)
   ```bash
   npm run process-images
   ```

3. **Generate dynamic content:**
   ```bash
   node scripts/generate-districts.js
   node scripts/generate-blog.js
   node scripts/refine-services.js
   ```

4. **Develop locally:**
   ```bash
   npm run dev
   ```

## 🏗️ Build & Deploy

### Building for Production
```bash
npm run build
```
The output will be in the `dist/` directory.

### cPanel Deployment
1. Connect to your server via FTP or File Manager.
2. Navigate to the `public_html` directory.
3. Upload all contents from the local `dist/` folder to `public_html`.
4. Ensure the `.htaccess` file is uploaded to the root.

## 📁 Key Directories
- `src/content/`: All markdown content for services, districts, and blog.
- `src/components/`: Reusable UI components.
- `public/assets/img/`: Optimized images (generated by pipeline).
- `scripts/`: Automation scripts for content and images.

## 🛠️ Tech Stack
- **Framework:** [Astro 5](https://astro.build/)
- **Styling:** [Tailwind CSS](https://tailwindcss.com/)
- **Icons:** Inline SVG
- **Theme:** Dark/Gold Premium Aesthetic

## 📈 SEO Checklist
- [ ] Check `src/consts.ts` for global site metadata.
- [ ] Verify `public/robots.txt`.
- [ ] Review `redirect-map.json` and update `.htaccess` with 301 rules for old WordPress URLs.
- [ ] Submit `sitemap-index.xml` to Google Search Console after launch.
